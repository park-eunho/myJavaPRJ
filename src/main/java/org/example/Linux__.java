package org.example;

import com.sun.javafx.scene.control.behavior.SliderBehavior;

public class Linux__ {
    cd
    pwd
    cd ../usr/lib
    cd ../home/ubuntu
    ls -l /etc/hosts
    groups root
    mkdir ch5
    cd ch5
    touch test.txt
    ls -l
    chmod u-w test.txt
    ls -l
}
